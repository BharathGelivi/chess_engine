# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust engine build output (cargo). public/engine/pkg (wasm-pack's output)
# IS checked in deliberately: Vercel's build has no Rust toolchain, so this
# small (~85KB) compiled artifact must be committed to deploy at all.
engine/target

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
